import Timer from "../../sections/Timer/Timer.tsx";
import MainTemplate from "../../templates/MainTemplate/index.tsx";

const Home = () => {
  return (
    <MainTemplate>
      <Timer />
    </MainTemplate>
  );
};

export default Home;
